<Window x:Class="RussianNationalMessengerClient.Views.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RussianNationalMessengerClient.Views.Windows"
        xmlns:viewModels="clr-namespace:RussianNationalMessengerClient.ViewModels"
        xmlns:views="clr-namespace:RussianNationalMessengerClient.Views.Pages"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>

        <DataTemplate
            DataType="{x:Type viewModels:AuthViewModel}">

            <views:AuthPage/>
        </DataTemplate>
        
        <DataTemplate
            DataType="{x:Type viewModels:ChatsViewModel}">

            <views:ChatsPage/>
        </DataTemplate>

    </Window.Resources>

    <Grid>
        <ContentControl
            Content="{Binding CurrentViewModel}"/>
    </Grid>
    <!--<Border BorderBrush="gray" BorderThickness="0 0 2 0">
            <Grid RowDefinitions="auto, *">
                <Grid ColumnDefinitions="*, *, *" Margin="5">
                    <Button HorizontalAlignment="Left">Меню</Button>
                    <TextBox Grid.Column="1" Grid.ColumnSpan="2"/>
                </Grid>
                <ListView Grid.Row="1" ItemsSource="{Binding Chats}"
                          HorizontalContentAlignment="Stretch">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Button Background="Transparent" BorderThickness="0" Command="{Binding ShowDialogCommand}">
                                <Button.Content>
                                    <Label Content="{Binding .}"/>
                                </Button.Content>
                            </Button>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Border>

    -->
        
</Window>
